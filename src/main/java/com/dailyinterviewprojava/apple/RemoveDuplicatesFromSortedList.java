package com.dailyinterviewprojava.apple;

/**
 * 
 * @author ema
 * Given a sorted list of size n, with m unique elements (thus m < n), modify the list such that the first m unique elements in the list will be sorted, 
 * ignoring the rest of the list. Your solution should have a space complexity of O(1). 
 * Instead of returning the list (since you are just modifying the original list), you should return what m is.
 * 
 */
public class RemoveDuplicatesFromSortedList {

}
